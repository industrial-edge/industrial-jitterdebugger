[tool.poetry]
name = "stress"
version = "0.1.0"
description = "stress-ng tool of cpu-real-time-test project"
authors = ["siemens"]

[tool.poetry.dependencies]
python = "^3.9"
flask = "^3.0.3"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
